The verification process will be computerized. The terminal dials the ChocAn Data Center. The authentication. should be treated the same way. Each member report, provider report, and provider directory must be written to its own file. As for the EFT data, all that is required is that a file be set up containing the provider name, provider number, and the amount to be transferred. User Management will be saved in a dictionary containing emails and passwords. Hardware will be needed to contain these files and send data to the terminal when needed. Terminal will dial other systems as needed. Users will enter information on the terminal. 